name: Generate ATT&CK Data

on:
  schedule:
    - cron: "0 3 * * *"
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python Environment
      uses: actions/setup-python@v1
      with:
        python-version: 3.8
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -U -r requirements.txt
    - name: Generate ATTCK Data
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "running run.py"
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.SWIMLANE_PYATTCK_AWS_ACCESS_KEY }}
        aws-secret-access-key: ${{ secrets.SWIMLANE_PYATTCK_AWS_SECRET_KEY }}
        aws-region: us-west-2
    - name: Upload files to S3 with AWS CLI
      run: |
        echo $PWD
        ls -al
        aws s3 sync . s3://${{ secrets.SWIMLANE_PYATTCK_AWS_BUCKET_NAME }} --exclude "*" --include "*.json" --delete 
  